# -*- mode: python -*-
Import("env")

env = env.Clone()

env.CppUnitTest(
    target="sasl_oidc_test",
    source=[
        "sasl_oidc_server_mechanism_test.cpp",
    ],
    LIBDEPS=[
        "$BUILD_DIR/mongo/crypto/jwt",
        "externalsaslauth",
    ],
)
